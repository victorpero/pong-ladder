/**
 * The white app header the logo sits on. Used for the browser theme colour and
 * for the installed app's splash screen, so the status bar and the first
 * painted screen agree.
 */
export const APP_THEME_COLOR = "#ffffff";
