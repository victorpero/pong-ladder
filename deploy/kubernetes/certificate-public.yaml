apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pongladder-com
  namespace: pingpong
spec:
  secretName: pongladder-com-tls
  dnsNames:
    - pongladder.com
    - www.pongladder.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
